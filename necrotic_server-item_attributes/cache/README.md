# Server Cache

The full Psycho server cache is intentionally not committed to regular git because several cache files exceed GitHub's per-file limits.

Keep the local cache files in this directory for development. For hosted distribution, publish the cache through release artifacts, external object storage, or Git LFS.
